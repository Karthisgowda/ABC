# Workflow

Use this quick flow for normal project work:

1. Record useful activity with `npm run log -- "message"`.
2. Review totals with `npm run stats` or `npm run days`.
3. Run `npm test`.
4. Commit and push the actual project changes.

Generated report files are for local review and are ignored by git.
